{"index.js":"function telephoneCheck(str) {\n  //negatives! Let's get rid of them\n  if(str[0] === \"-\"){\n    console.log(\"started Negative\");\n    return false;\n  }\n  \n  let phone = str.replace(/[^(\\d|(|))]/g, '');\n  let phoneNum = str.replace(/[^\\d]/g, '');\n  console.log(phoneNum);\n  //figure out those parenthesis\n  if((phone.match(/[\\(|\\)]/g)))\n  {\n    console.log(\"Paren check\");\nvar count = (phone.match(/[\\(|\\)]/g)).length;\n  if(count !==2)\n  {\n    console.log(\"wrong number of parenthesis\");\n    return false;\n  }\n  //are in paren in wrong place?\n  else if(phone.indexOf(\")\") - phone.indexOf(\"(\") > 4){\n    console.log(\"bad paren placement\");\n    return false;\n  }\n  }\n//is phone number too long?\nconsole.log(\"No parenthesis\");\n  if(phoneNum.length != 11 && phoneNum.length != 10){\n    console.log(phoneNum.length);\n    console.log(\"Invalid length\");\n    return false;\n  } // if its longer, is first char a proper country code?\n  else if(phoneNum.length === 11){\n    console.log(\"correct length\");\n    if(phoneNum[0] !== '1'){\n      console.log(\"Invalid start\");\n      return false;\n    }\n  }\n  console.log(phone);\n   \n  \n  console.log(count);\n  // Good luck!\n  return true;\n}\n\ntelephoneCheck(\"0 (757) 622-7382\");"}